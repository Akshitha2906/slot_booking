services:
  - type: web
    name: slot-booking
    runtime: python
    buildCommand: sh build.sh
    startCommand: gunicorn slotproject.wsgi
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: slot-booking-db
          property: connectionString
